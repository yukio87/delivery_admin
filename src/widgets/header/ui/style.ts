import styled from 'styled-components'

export const Header = styled.header`
  position: relative;
  display: grid;
  grid-template-columns: 1fr auto auto;
  box-shadow: 5px 1px 10px var(--c-gray-200);
`
